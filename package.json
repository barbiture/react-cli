{
  "name": "react-cli",
  "version": "1.0.0",
  "description": "create component style test example",
  "main": "index.js",
  "author": "rif",
  "license": "MIT",
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore ."
  },
  "dependencies": {
    "eslint": "^6.8.0",
    "eslint-config-strongloop": "^2.1.0",
    "prettier": "^2.0.5"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "install": "^0.13.0"
  }
}
